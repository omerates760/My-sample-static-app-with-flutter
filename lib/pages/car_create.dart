import 'package:flutter/material.dart';

class CarCreatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("araba oluştur."),
    );
  }
}
